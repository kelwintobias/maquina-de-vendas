{
  "name": "valeria-evolution",
  "version": "1.0.0",
  "description": "ValerIA WhatsApp Agent - Evolution API Backend",
  "scripts": {
    "setup": "pip install -r requirements.txt",
    "bot": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "tunnel": "ngrok http 8000",
    "start": "concurrently --names bot,tunnel --prefix-colors blue,green \"npm run bot\" \"npm run tunnel\"",
    "test": "python -m pytest tests/ -v",
    "ngrok:auth": "ngrok config add-authtoken"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "ngrok": "^5.0.0-beta.2"
  }
}
