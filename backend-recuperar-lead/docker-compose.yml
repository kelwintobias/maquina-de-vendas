services:
  api:
    image: canastra-sdr:latest
    networks:
      - canastrainteligencia
    env_file: .env
    # depends_on is ignored by docker stack deploy — app must handle Redis unavailability
    depends_on:
      - redis
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
        failure_action: rollback
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=canastrainteligencia"
        - "traefik.http.routers.sdr.rule=Host(`sdr.canastrainteligencia.com`)"
        - "traefik.http.routers.sdr.entrypoints=websecure"
        - "traefik.http.routers.sdr.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.sdr.loadbalancer.server.port=8000"

  worker:
    image: canastra-sdr:latest
    command: python -m app.campaign.worker
    networks:
      - canastrainteligencia
    env_file: .env
    # depends_on is ignored by docker stack deploy — worker must handle Redis unavailability
    depends_on:
      - redis
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
        failure_action: rollback

  redis:
    image: redis:7-alpine
    networks:
      - canastrainteligencia
    volumes:
      - sdr_redis_data:/data
    deploy:
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 5

networks:
  canastrainteligencia:
    external: true

volumes:
  sdr_redis_data:
